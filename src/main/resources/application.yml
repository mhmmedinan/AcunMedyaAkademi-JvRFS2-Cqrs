server:
    port: 8080
spring:
    application:
         name: rentacarcqrs
    datasource:
         password: 12345
         url: jdbc:postgresql://localhost:5432/rentacarcqrs
         username: postgres
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                 dialect: org.hibernate.dialect.PostgreSQLDialect
            javax:
                persistence:
                    validation:
                          mode: none
            show-sql: true